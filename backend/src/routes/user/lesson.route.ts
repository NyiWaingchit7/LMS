import express from "express";
import { index, show } from "@/controller/lesson.controller";

export const userLessonRouter = express.Router();

// userLessonRouter.get("/", index);

// userLessonRouter.get("/:id", show);
